[project]
name = "eclipse-ditto-sionnart"
version = "1.0"
description = "RF coverage simulation using SionnaRT ray-tracing with Eclipse Ditto digital twin integration."
readme = "README.md"
requires-python = ">=3.10, <3.12"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Development Status :: 3 - Alpha",
]
dependencies = [
    "sionna-rt==1.2.1",
    "matplotlib==3.10.7",
    "mitsuba",
    "numpy",
    "loguru",
    "PyYAML",
    "pydantic==2.12.4",
    "pyproj",
    "scene_generation",
    "scikit-learn",
    "trimesh",
    "rasterio",
    "requests",
    "shapely",
    "osmnx",
    "geopandas",
    "tqdm",
    "urllib3",
]

[project.optional-dependencies]
worldcover_ndvi = [
    "pystac-client",
    "planetary-computer",
]

[dependency-groups]
dev = ["ruff", "pytest"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.uv.sources]
scene_generation = { git = "https://github.com/functions-lab/geo2sigmap.git", subdirectory = "package" }
